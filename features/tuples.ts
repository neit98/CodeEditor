const drink = {
  color: 'brown',
  carbonated: true,
  sugar: 40,
};

type Drink = [string, boolean, number];

const pepsi: Drink = ['brown', true, 40];
const coke: Drink = ['red', false, 21];
const tea: Drink = ['yellow', true, 21];

const carSpecs: [number, number] = [400, 2323];

const carStats = {
  horsepower: 400,
  weight: 2323,
};
